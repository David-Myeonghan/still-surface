{
  "name": "still-surface",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "node --test",
    "smoke": "node tools/smoke.mjs"
  },
  "dependencies": { "three": "^0.185.1" },
  "devDependencies": { "vite": "^8.1.5", "playwright": "^1.62.0" }
}
